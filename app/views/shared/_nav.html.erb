<nav>
  <section class="logo">
    <a href="/"><img src="/img/logo.png">ArborShare</a>
  </section>
  <section class="session_control">
  <% if current_user %>
    <% if current_admin? %>
      <%= link_to 'Admin Dashboard', admin_dashboard_path %>
    <% end %>
    <%= link_to 'Logout', '/logout' %>
  <% else %>
    <%= render partial: 'shared/google_login' %>
  <% end %>
  </section>
</nav>
